
{
  "name": "transporte-beilus",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "sass:build": "sass scss/main.scss css/main.css --no-source-map --style=compressed",
    "sass:watch": "sass --watch scss/main.scss css/main.css --style=expanded"
  },
  "devDependencies": { "sass": "^1.77.6" }
}
